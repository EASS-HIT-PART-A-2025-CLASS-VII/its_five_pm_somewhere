version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8001:8000"
    networks:
      - app-network
    # depends_on:
    #   - ai-service
    env_file:
      - ./backend/.env

  # ai-service:
  #   build: ./ai-service
  #   ports:
  #     - "8002:8000"
  #   networks:
  #     - app-network

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_BACKEND_URL=http://backend:8000
    networks:
      - app-network
    depends_on:
      - backend

networks:
  app-network:
